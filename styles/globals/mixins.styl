btn(color, bg, border, shadow)
	color: color
	if shadow
		box-shadow: 0px 2px 4px 0px rgba(0,0,0,0.24)
	background-color: bg
	if border
		border: 1px solid border
	else
		border: none
	&:focus
		outline: 0px
	&:hover
		background-color: darken(bg, 30%)
	&:active
		box-shadow: inset 0 10px 5px rgba(0,0,0,.125)